<?php
$inputString = "Tous ces instants seront perdus dans le temps. Comme les larmes sous la pluie. Une autre phrase ici.";

// Initialiser le compteur et l'index
$count = 0;
$i = 0;


while ($i < isset($count($inputString))){
        continue;
    }

    echo $inputString[$i];
    
    if ($inputString[$i] === '.') {
        echo $inputString[$i];
    }

    // Incrémenter le compteur et l'index
    $count++;
    $i++;

echo "\nNombre de caractères : " . $count;
?>
